# --- RPC (Sepolia + mainnet) ---
NEXT_PUBLIC_ALCHEMY_API_KEY=        # or use full RPC URLs below
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
MAINNET_RPC_URL=https://ethereum-rpc.publicnode.com

# --- Wallet ---
NEXT_PUBLIC_WALLETCONNECT_ID=       # RainbowKit / WalletConnect project id

# --- Faucet deploy (Sepolia only; never commit a real key) ---
DEPLOYER_PRIVATE_KEY=

# --- Optional ---
ETHERSCAN_API_KEY=                  # contract verification
# Relayer/gateway URLs per chain come from the SDK chain presets.
# Sepolia relayer (reference): https://relayer.testnet.zama.org
